// Empty Array Declaration
let emptyArray = [];

// Array with Initial Elements
let arrayWithElements = [1, 2, 3, 4, 5];

// Using the Array Constructor
let arrayConstructor = new Array();

// Array Constructor with Size
let arrayConstructorWithSize = new Array(5); // Creates an array with length 5

// Array Constructor with Initial Elements
let arrayConstructorWithElements = new Array('apple', 'banana', 'orange');

// Using Array.from() method: Converts a string into an array of characters
let fromMethod = Array.from('hello');

// Using Array.of() method: Creates an array from given arguments
let ofMethod = Array.of(1, 2, 3, 4, 5);
