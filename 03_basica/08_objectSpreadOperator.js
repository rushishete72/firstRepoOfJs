/**
 * Object Spread Operator: The spread operator (...) can be used to create a new object by merging properties from existing
 * objects or adding new properties.
 * Example:
 */
let newObj = { ...person, city: "Los Angeles" };
