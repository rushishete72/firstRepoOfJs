/**
 * Definition: Functions in JavaScript are blocks of reusable code that perform a specific task. They can take input parameters
 * and return a value. Functions are first-class citizens in JavaScript, meaning they can be assigned to variables, passed as
 * arguments to other functions, and returned from other functions.
 */
