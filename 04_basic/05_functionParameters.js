/**
 * Function Parameters: Functions in JavaScript can take zero or more parameters, which are specified inside the parentheses
 * in the function declaration or expression. Parameters are placeholders for values that will be passed to the function when it
 * is called.
 * Example:
 */
function add(a, b) {
    return a + b;
}
