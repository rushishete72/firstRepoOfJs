/**
 * Function Return: Functions in JavaScript can return a value using the return statement. The return statement stops the execution
 * of the function and returns the specified value to the caller. If no return value is specified, the function returns undefined.
 * Example:
 */
function add(a, b) {
    return a + b;
}
